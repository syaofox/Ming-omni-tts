services:
  ming-omni-tts:
    build:
      context: .
      dockerfile: docker/ming_uniaudio.dockerfile
      args:
        - UID=${UID:-1000}
        - GID=${GID:-1000}
    container_name: ming-omni-tts
    # runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - .:/app
      - ./models:/app/models
      - ./output:/app/output
      - ./data:/app/data
      - ${HOME}/.config/opencode:/home/appuser/.config/opencode
      - ${HOME}/.local/share/opencode:/home/appuser/.local/share/opencode
      - ${HOME}/.local/state/opencode:/home/appuser/.local/state/opencode
      - ./AGENTS.md:/app/AGENTS.md
    working_dir: /app
    stdin_open: true
    tty: true
    user: "${UID:-1000}:${GID:-1000}"
